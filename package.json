{
  "name": "feather-mysql",
  "version": "0.2.0",
  "private": true,
  "description": "Independent FiveM/RedM Lua database resource",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:lua": "python3 tests/run_lua.py",
    "test:integration": "python3 tests/run_integration.py",
    "verify": "npm test && npm run test:lua && npm run test:integration"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "mysql2": "3.24.4" }
}
